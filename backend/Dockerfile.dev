# Use a lightweight official Python image
FROM python:3.11-slim

# Set working directory inside the container
WORKDIR /app

# Set environment variables to prevent Python from buffering stdout/stderr
ENV PYTHONDONTWRITEBYTECODE 1
ENV PYTHONUNBUFFERED 1

# Copy dependencies list and install them
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# The ENTRYPOINT is what runs when the container starts.
# We'll use uvicorn with the --reload flag for local development.
# The code is NOT copied here; it's mounted via Docker Compose volumes later.
# We use 'app.main:app' assuming your FastAPI app is defined in main.py as 'app'.
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]

# Expose the internal container port
EXPOSE 8000